---
phase: 02-memory-agent-automation-script
plan: 01
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - source-code/memory_agent.py
  - .agents/skills/memory-agent/SKILL.md
  - .agents/settings.json
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03
  - AGT-04

must_haves:
  truths:
    - "source-code/memory_agent.py exists and can be executed"
    - "memory-agent local skill configured in .agents/skills/memory-agent/SKILL.md"
  artifacts:
    - path: "source-code/memory_agent.py"
      provides: "Python Memory Agent automation logic"
    - path: ".agents/skills/memory-agent/SKILL.md"
      provides: "Skill manifest for local skill integration"
---

<objective>
Implement the Python Memory Agent automation script, package it as a local Antigravity/Gemini skill, and configure automated hook triggers.

Purpose: Automate the process of reading walkthrough summaries and git commits to keep the brain memory files consistently up to date.
Output: memory_agent.py script, local skill folder/SKILL.md, and updated settings.json/git hooks.
</objective>

<execution_context>
@C:/Users/rohit/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/rohit/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-agent-automation-script/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Python Memory Agent script</name>
  <files>source-code/memory_agent.py</files>
  <action>Write memory_agent.py. It should:
1. Fetch latest git commit message and diff.
2. Search for any summary/walkthrough file in .planning/ or .planning/phases/.
3. Extract new patterns, decisions, and mistakes.
4. Construct an API payload to OpenRouter free models using urllib.request.
5. Merge changes into brain/ memory files and write them to disk.
6. Compress total brain memory into brain/master-memory.md under 5,000 words.
7. Read OPENROUTER_API_KEY from environment or .env file.</action>
  <verify>Run memory_agent.py directly and verify it executes without syntax/runtime errors</verify>
  <done>source-code/memory_agent.py operates without errors</done>
</task>

<task type="auto">
  <name>Task 2: Register local skill in Antigravity</name>
  <files>.agents/skills/memory-agent/SKILL.md</files>
  <action>Create .agents/skills/memory-agent/SKILL.md describing the memory-agent skill, exposing arguments and options to run the Python script.</action>
  <verify>Check if skill is correctly parsed by running a dry run command</verify>
  <done>.agents/skills/memory-agent/SKILL.md exists with correct metadata header</done>
</task>

<task type="auto">
  <name>Task 3: Configure hooks for automatic updates</name>
  <files>.agents/settings.json, .git/hooks/post-commit</files>
  <action>Update .agents/settings.json to add a session hook executing the memory agent script, and write a git post-commit hook calling it automatically after commits.</action>
  <verify>Test hook triggers</verify>
  <done>Hooks successfully configured to trigger memory_agent.py</done>
</task>

</tasks>

<verification>
- [ ] Python memory agent executes successfully
- [ ] Local skill is properly registered
- [ ] Post-commit and settings hooks are correctly configured
</verification>

<success_criteria>
- Python script created and runnable
- local skill config created
- settings.json and git hooks updated
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-agent-automation-script/02-01-SUMMARY.md`
</output>
